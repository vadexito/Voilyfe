<?php 
$this->headLink()->prependStylesheet('/css/lib/datebox.css');
$this->headScript()->offsetSetFile(100,'https://www.google.com/jsapi'); 
$this->headScript()->offsetSetFile(101,'/js/lib/jqm-datebox/datebox-core.js');
$this->headScript()->offsetSetFile(102,'/js/lib/jqm-datebox/datebox-core-modeCalbox.js');

if ($this->events)
{
    echo $this->subPageIndex($this->category,'last-events-page','page',
           $this->partial('partial/_pageLastEvents-mobile.phtml',['events' => $this->events,'all'=>$this->allOption])
    );
    echo $this->subPageIndex($this->category,'calendar-page','calendar',
           $this->partial('partial/_pageCalendar-mobile.phtml',['events' => $this->events,'eventsPerDay' => $this->eventsPerDay])
    );
    
    echo $this->subPagesGraphIndex($this->category,$this->graphs($this->events,$this->allOption));
    
} 
else //if no event to show
{
    echo $this->subPageIndex($this->category,'last-events-page','page',
           '<p>'.$this->translate('In order to add some events press the plus button')
            .'</p>',
            ''
    );
}
    echo $this->subPageIndex($this->category,'list_singleCategories','',
           $this->listCategories('partial/_categoriesNewEventMenu.phtml','eventAdd'),
           '',
           ucfirst($this->translate('menu_add_event'))
    );
    
    echo $this->partial('partial/_mobilePage.phtml', array(
        'id'         => 'list_allCategories',
        'content'    => $this->listCategories('partial/_categoriesMenu.phtml','metaCategory'),
        'title'      => ucfirst($this->translate('menu_see_categories')),
        'buttonLeft' => 'back',
        'footer'     => '_footerCategories.mobile'
    )); 
    
    echo $this->subPageIndex($this->category,'options-page','options',
           $this->settingsMobile(),
           '',
           ucfirst($this->translate('menu_settings'))
    );
?>
     
    <span id="dataToClientSide" data-categoryId="<?php echo $this->category->id; ?>"></span>

